<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Britium Gallery | Luxury Handbags</title>

    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">

    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --bg-dark: #1a0f0a;
            --bg-cream: #f9f4ef;
            --gold-accent: #debb6e;
            --gold-dark: #b5904b;
            --text-light: #ffffff;
            --text-dark: #2c241b;
            --chat-bg: #231712;
        }

        body {
            font-family: 'Lato', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-light);
            line-height: 1.6;
        }

        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }
        a:hover { color: var(--gold-accent); }

        /* HEADER */
        header {
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(212,175,55,0.2);
        }

        .logo {
            font-size: 1.5rem;
            color: var(--gold-accent);
            font-weight: 700;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        /* HERO */
        .hero {
            text-align: center;
            padding: 90px 20px 50px;
            background: linear-gradient(180deg, rgba(26,15,10,1) 0%, rgba(40,25,18,1) 100%);
        }

        .hero h1 {
            font-size: 3.5rem;
            color: var(--gold-accent);
            margin-bottom: 10px;
            line-height: 1.2;
        }

        .hero p {
            font-size: 1.2rem;
            color: #e0e0e0;
            font-weight: 300;
        }

        .btn-gold {
            background-color: var(--gold-accent);
            color: var(--bg-dark);
            padding: 15px 40px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            cursor: pointer;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-top: 35px;
            transition: 0.3s;
        }

        .btn-gold:hover {
            background-color: var(--gold-dark);
            transform: translateY(-2px);
        }

        /* COLLECTION */
        .collection {
            padding: 60px 5%;
            text-align: center;
        }

        .collection h3 {
            color: var(--gold-accent);
            margin-bottom: 10px;
        }

        .collection-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .product-card {
            background: rgba(255,255,255,0.05);
            padding: 20px;
            border: 1px solid rgba(212,175,55,0.1);
            transition: 0.3s;
        }

        .product-card:hover {
            border-color: var(--gold-accent);
        }

        .placeholder-img {
            width: 100%;
            height: 260px;
            background-color: #2c241b;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gold-accent);
            margin-bottom: 15px;
            font-style: italic;
        }

        .product-title {
            font-size: 1.1rem;
            color: var(--gold-accent);
            margin-bottom: 5px;
        }

        .product-price {
            font-size: 0.9rem;
            color: #ccc;
        }

        /* BRAND SIGNATURE */
        .brand-signature {
            background-color: var(--bg-cream);
            color: var(--text-dark);
            text-align: center;
            padding: 80px 20px;
            margin-top: 60px;
            border-top: 5px solid var(--gold-accent);
        }

        .brand-signature h2 {
            font-size: 3rem;
            color: var(--gold-dark);
        }

        .brand-signature h3 {
            font-size: 2rem;
            font-style: italic;
        }

        footer {
            background-color: #0d0705;
            color: #666;
            text-align: center;
            padding: 30px;
            font-size: 0.8rem;
        }

        @media(max-width:768px) {
            nav ul { display: none; }
            .hero h1 { font-size: 2.5rem; }
        }
    </style>
</head>

<body>

<header>
    <div class="logo">BRITIUM GALLERY</div>
   <nav>
  <ul>
    <li><a href="index.html">Home</a></li>
    <li><a href="shop.html">Shop</a></li>
    <li><a href="new-arrivals.html">New Arrivals</a></li>
    <li><a href="luxury-handbags.html">Luxury Handbags</a></li>
    <li><a href="product-gallery.html">Gallery</a></li>
    <li><a href="about.html">About</a></li>
    <li><a href="blog.html">Blog</a></li>
    <li><a href="faq.html">FAQ</a></li>
    <li><a href="contact.html">Contact</a></li>
    <li><a href="login.html">Login</a></li>
  </ul>
</nav>

    <div style="color:var(--gold-accent)">🛒 (0)</div>
</header>

<section class="hero">
    <h1>LUXURY HANDBAGS</h1>
    <p>Crafted icons of elegance, precision, and enduring luxury.</p>
    <button class="btn-gold" onclick="location.href='shop.html'">Explore Collection</button>
</section>

<section class="collection">
    <h3>The Signature Edit</h3>

    <div class="collection-grid">

        <div class="product-card">
            <div class="placeholder-img">[Ivory Tote]</div>
            <div class="product-title">Ivory Elegance Tote</div>
            <div class="product-price">MMK 125,000.00</div>
        </div>

        <div class="product-card">
            <div class="placeholder-img">[Quilted Satchel]</div>
            <div class="product-title">Noir Quilted Satchel</div>
            <div class="product-price">MMK 145,000.00</div>
        </div>

        <div class="product-card">
            <div class="placeholder-img">[Monogram Handbag]</div>
            <div class="product-title">Signature Monogram Handbag</div>
            <div class="product-price">MMK 110,000.00</div>
        </div>

        <div class="product-card">
            <div class="placeholder-img">[Structured Mini]</div>
            <div class="product-title">V-Lock Structured Mini</div>
            <div class="product-price">MMK 98,000.00</div>
        </div>

        <div class="product-card">
            <div class="placeholder-img">[Chain Shoulder]</div>
            <div class="product-title">Gold Chain Shoulder Bag</div>
            <div class="product-price">MMK 135,000.00</div>
        </div>

        <div class="product-card">
            <div class="placeholder-img">[Classic Satchel]</div>
            <div class="product-title">Heritage Leather Satchel</div>
            <div class="product-price">MMK 120,000.00</div>
        </div>

    </div>
</section>

<section class="brand-signature">
    <h2>BRITIUM GALLERY</h2>
    <h3>Where Style Meets Innovation</h3>
</section>

<footer>
    <p>&copy; 2025 Britium Gallery. All Rights Reserved.</p>
</footer>

</body>
</html>
