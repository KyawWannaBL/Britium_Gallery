<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Britium Gallery</title>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <style>
        /* --- VARIABLES & RESET --- */
        :root {
            --bg-black: #050505;
            --input-bg: #0f0f0f;
            --border-color: #333333;
            --text-white: #ffffff;
            --text-grey: #a0a0a0;
            --accent-gold: #d4af37; /* For the storefront text simulation */
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Jost', sans-serif;
            background-color: var(--bg-black);
            color: var(--text-white);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }

        /* --- MAIN WRAPPER --- */
        .login-wrapper {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 40px 20px;
            max-width: 100%;
        }

        /* --- LOGO SECTION --- */
        .logo-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 40px;
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        /* CSS Shape to mimic the Logo Icon */
        .logo-icon {
            width: 40px;
            height: 40px;
            position: relative;
        }
        .logo-icon::before, .logo-icon::after {
            content: '';
            position: absolute;
            border-style: solid;
        }
        /* Simplified geometric triangle/arrow shape */
        .logo-icon::before {
            top: 0; left: 0;
            border-width: 20px 0 20px 30px;
            border-color: transparent transparent transparent white;
        }
        .logo-icon::after {
            top: 10px; left: 5px;
            border-width: 10px 0 10px 15px;
            border-color: transparent transparent transparent var(--bg-black);
        }

        .logo-text {
            text-transform: uppercase;
            font-weight: 700;
            font-size: 1.8rem;
            letter-spacing: 2px;
            line-height: 1;
        }
        .logo-sub {
            font-size: 0.9rem;
            letter-spacing: 4px;
            font-weight: 400;
            margin-top: 2px;
            text-align: center;
        }

        /* --- LOGIN FORM --- */
        h1 {
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 30px;
        }

        .login-form {
            width: 100%;
            max-width: 400px;
        }

        .input-group {
            position: relative;
            margin-bottom: 20px;
        }

        input {
            width: 100%;
            padding: 18px 20px;
            background-color: transparent;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            color: var(--text-white);
            font-size: 1rem;
            font-family: inherit;
            outline: none;
            transition: 0.3s;
        }

        input:focus {
            border-color: #666;
            background-color: var(--input-bg);
        }

        /* Forgot Password Link inside Input */
        .forgot-pass {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 0.85rem;
            color: var(--text-white);
            font-weight: 500;
        }
        .forgot-pass:hover { text-decoration: underline; }

        .btn-login {
            width: 100%;
            padding: 18px;
            background-color: #111; /* Slightly lighter black for button */
            color: var(--text-white);
            border: none;
            border-radius: 6px;
            font-size: 1.1rem;
            font-weight: 500;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }
        .btn-login:hover {
            background-color: #222;
        }

        .signup-text {
            margin-top: 25px;
            font-size: 1rem;
            color: var(--text-white);
            text-align: center;
        }
        .signup-text a { font-weight: 600; }
        .signup-text a:hover { text-decoration: underline; }

        /* --- BOTTOM HERO IMAGE --- */
        .storefront-hero {
            width: 100%;
            height: 45vh; /* Takes up bottom half roughly */
            position: relative;
            overflow: hidden;
        }

        .storefront-hero img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center top;
            display: block;
        }

        /* Optional: Gradient overlay to blend image with black background */
        .storefront-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100px;
            background: linear-gradient(to bottom, var(--bg-black), transparent);
            z-index: 2;
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            h1 { font-size: 2rem; }
            .storefront-hero { height: 35vh; }
            .login-wrapper { padding-top: 60px; }
        }
    </style>
</head>
<body>

    <div class="login-wrapper">
        <div class="logo-section">
            <div class="logo-container">
                <div class="logo-icon"></div> <div>
                    <div class="logo-text">BRITIUM</div>
                    <div class="logo-sub">GALLERY</div>
                </div>
            </div>
        </div>

        <h1>Login</h1>

        <form class="login-form">
            <div class="input-group">
                <input type="email" placeholder="Email" required>
            </div>

            <div class="input-group">
                <input type="password" placeholder="Password" required>
                <a href="#" class="forgot-pass">Forgot password?</a>
            </div>

            <button type="submit" class="btn-login">Login</button>

            <div class="signup-text">
                Don't have an account? <a href="#">Sign up</a>
            </div>
        </form>
    </div>

    <div class="storefront-hero">
        <img src="images/storefront-gold-bg.jpg" alt="Britium Gallery Storefront">
    </div>

</body>
</html>
