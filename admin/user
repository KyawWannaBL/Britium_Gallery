<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Customers | Admin Panel</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    body{
      margin:0;
      font-family:Inter, sans-serif;
      background:#f8fafc;
      color:#0f172a;
    }

    main{
      margin-left:240px;
      padding:110px 40px 40px;
    }

    h1{
      font-size:1.8rem;
      margin-bottom:6px;
    }

    .muted{
      color:#64748b;
      margin-bottom:30px;
    }

    /* TOOLBAR */
    .toolbar{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:16px;
      flex-wrap:wrap;
      margin-bottom:25px;
    }

    .toolbar input{
      padding:10px 14px;
      border-radius:8px;
      border:1px solid #e5e7eb;
      font-size:.9rem;
      background:#fff;
      width:280px;
    }

    .btn{
      padding:10px 16px;
      border-radius:8px;
      border:1px solid #c7d2fe;
      background:#eef2ff;
      color:#1e3a8a;
      font-size:.85rem;
      font-weight:600;
      cursor:pointer;
    }

    .btn:hover{
      background:#e0e7ff;
    }

    /* TABLE PANEL */
    .panel{
      background:#fff;
      border-radius:14px;
      padding:24px;
      border:1px solid #e5e7eb;
      box-shadow:0 10px 30px rgba(0,0,0,.04);
    }

    table{
      width:100%;
      border-collapse:collapse;
      font-size:.95rem;
    }

    th, td{
      padding:14px 10px;
      border-bottom:1px solid #e5e7eb;
      text-align:left;
    }

    th{
      text-transform:uppercase;
      font-size:.75rem;
      letter-spacing:.08em;
      color:#64748b;
    }

    tr:hover td{
      background:#f9fafb;
    }

    .status{
      padding:4px 10px;
      border-radius:999px;
      font-size:.75rem;
      font-weight:600;
    }

    .active{ background:#dcfce7; color:#166534; }
    .inactive{ background:#fee2e2; color:#991b1b; }

    .role{
      font-weight:600;
      color:#334155;
    }
  </style>
</head>

<body>

  <!-- ADMIN NAV (AUTO-INJECTED) -->
  <div id="adminHeader"></div>
  <div id="adminSidebar"></div>

  <main>
    <h1>Customers</h1>
    <p class="muted">Manage registered users and access levels</p>

    <!-- TOOLBAR -->
    <div class="toolbar">
      <input type="text" placeholder="Search by name or email">
      <button class="btn">Add Customer</button>
    </div>

    <!-- TABLE -->
    <section class="panel">
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Kyaw Wanna</td>
            <td>mgkyawwanna@gmail.com</td>
            <td class="role">Admin</td>
            <td><span class="status active">Active</span></td>
          </tr>
          <tr>
            <td>Aye Myat</td>
            <td>ayemyat@gmail.com</td>
            <td class="role">Customer</td>
            <td><span class="status active">Active</span></td>
          </tr>
          <tr>
            <td>Min Thu</td>
            <td>minthu@gmail.com</td>
            <td class="role">Customer</td>
            <td><span class="status inactive">Inactive</span></td>
          </tr>
        </tbody>
      </table>
    </section>
  </main>

  <!-- ADMIN SCRIPT -->
  <script src="admin.js"></script>

</body>
</html>
