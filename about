<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us | Britium Gallery</title>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500&family=Playfair+Display:wght@400;600&display=swap" rel="stylesheet">
    
    <style>
        /* --- RESET & VARIABLES --- */
        :root {
            --bg-dark: #120e0b; /* Deep warm black/brown hue */
            --text-light: #f0f0f0;
            --text-grey: #cccccc;
            --accent-gold: #c6a87c;
            --gold-dark: #b5904b; /* Matches Index Page */
            --chat-bg: #231712;   /* Matches Index Page */
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Jost', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-light);
            line-height: 1.6;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }

        /* --- HEADER --- */
        header {
            background-color: #000;
            padding: 20px 60px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.3rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: #fff;
        }
        
        .logo-icon {
            width: 24px; height: 24px; 
            border: 2px solid #fff; 
            transform: rotate(45deg);
        }

        nav ul { display: flex; gap: 40px; list-style: none; }
        nav a { font-size: 0.9rem; font-weight: 500; color: #ccc; text-transform: uppercase; letter-spacing: 1px;}
        nav a:hover { color: #fff; }

        .header-icons { display: flex; gap: 20px; font-size: 1.2rem; cursor: pointer; }

        /* --- MAIN CONTAINER --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 60px 20px;
        }

        /* --- TOP SECTION --- */
        .about-intro {
            text-align: center;
            margin-bottom: 50px;
            max-width: 900px;
            margin-left: auto;
            margin-right: auto;
        }

        .about-intro h1 {
            font-family: 'Playfair Display', serif;
            font-size: 3.5rem;
            font-weight: 400;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        .about-intro p {
            font-size: 1.1rem;
            color: var(--text-grey);
            line-height: 1.8;
        }

        /* Showcase Image */
        .showcase-image {
            width: 100%;
            height: auto;
            margin-bottom: 80px;
            border-radius: 2px;
            background-color: #222;
            display: block;
        }

        /* --- CONTENT GRID (Story & Values) --- */
        .content-grid {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr; /* Text wider than image */
            gap: 60px;
            align-items: start;
        }

        /* Text Column */
        .text-column h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #fff;
        }

        .text-column p {
            font-size: 1rem;
            color: var(--text-grey);
            margin-bottom: 30px;
            text-align: justify;
        }

        .values-list {
            margin-top: 40px;
        }

        .value-item {
            margin-bottom: 20px;
        }
        
        .value-title {
            color: #fff;
            font-weight: 600;
            font-size: 1.05rem;
            margin-bottom: 5px;
            display: inline-block;
        }

        .value-desc {
            color: var(--text-grey);
            display: inline;
        }

        /* Image Column */
        .image-column {
            position: relative;
            height: 100%;
        }

        .image-column img {
            width: 100%;
            height: auto;
            object-fit: cover;
            border-radius: 2px;
        }

        /* --- FOOTER --- */
        footer {
            background-color: #0a0a0a;
            color: #666;
            text-align: center;
            padding: 40px 0;
            margin-top: 80px;
            border-top: 1px solid #222;
            font-size: 0.9rem;
        }

        /* --- CHATBOT WIDGET CSS --- */
        .chat-widget-btn {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background-color: var(--accent-gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(0,0,0,0.4);
            z-index: 1000;
            transition: transform 0.3s;
            border: 2px solid var(--gold-dark);
        }

        .chat-widget-btn:hover {
            transform: scale(1.1);
        }

        .chat-icon {
            font-size: 30px;
            color: var(--bg-dark);
        }

        .chat-window {
            position: fixed;
            bottom: 100px;
            right: 30px;
            width: 350px;
            height: 500px;
            background-color: var(--chat-bg);
            border: 1px solid var(--accent-gold);
            border-radius: 12px;
            display: none; /* Hidden by default */
            flex-direction: column;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            z-index: 1000;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.3s, transform 0.3s;
        }

        .chat-window.active {
            display: flex;
            opacity: 1;
            transform: translateY(0);
        }

        .chat-header {
            background-color: var(--bg-dark);
            padding: 15px;
            border-bottom: 1px solid var(--accent-gold);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .chat-header h4 {
            color: var(--accent-gold);
            font-family: 'Playfair Display', serif;
            font-size: 1rem;
            letter-spacing: 1px;
        }

        .close-chat {
            cursor: pointer;
            color: var(--accent-gold);
            font-size: 1.2rem;
        }

        .chat-messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .message {
            max-width: 80%;
            padding: 10px 15px;
            border-radius: 10px;
            font-size: 0.9rem;
            line-height: 1.4;
        }

        .message.bot {
            background-color: rgba(198, 168, 124, 0.1);
            color: #eee;
            border: 1px solid rgba(198, 168, 124, 0.3);
            align-self: flex-start;
            border-bottom-left-radius: 2px;
        }

        .message.user {
            background-color: var(--accent-gold);
            color: var(--bg-dark);
            align-self: flex-end;
            border-bottom-right-radius: 2px;
        }

        .chat-input-area {
            padding: 15px;
            background-color: var(--bg-dark);
            border-top: 1px solid rgba(198, 168, 124, 0.2);
            display: flex;
            gap: 10px;
        }

        .chat-input {
            flex: 1;
            padding: 10px;
            border-radius: 4px;
            border: 1px solid #444;
            background-color: #111;
            color: #fff;
            outline: none;
            font-family: 'Jost', sans-serif;
        }

        .chat-send {
            background-color: var(--accent-gold);
            color: var(--bg-dark);
            border: none;
            padding: 0 15px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 900px) {
            .content-grid { grid-template-columns: 1fr; gap: 40px; }
            .about-intro h1 { font-size: 2.5rem; }
        }
        @media (max-width: 600px) {
            header { padding: 20px; }
            nav ul { display: none; }
            .logo { font-size: 1.1rem; }
            .chat-window { width: 90%; right: 5%; bottom: 100px; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">
            <div class="logo-icon"></div>
            BRITIUM GALLERY
        </div>
        <nav>
  <ul>
    <li><a href="index.html">Home</a></li>
    <li><a href="shop.html">Shop</a></li>
    <li><a href="new-arrivals.html">New Arrivals</a></li>
    <li><a href="luxury-handbags.html">Luxury Handbags</a></li>
    <li><a href="product-gallery.html">Gallery</a></li>
    <li><a href="about.html">About</a></li>
    <li><a href="blog.html">Blog</a></li>
    <li><a href="faq.html">FAQ</a></li>
    <li><a href="contact.html">Contact</a></li>
    <li><a href="login.html">Login</a></li>
  </ul>
</nav>
        <div class="header-icons">
            <span>🔍</span>
            <span>👤</span>
        </div>
    </header>

    <div class="container">
        
        <section class="about-intro">
            <h1>ABOUT US</h1>
            <p>
                At Britium Gallery, we merge timeless elegance with modern sophistication. As a luxury fashion destination, we specialize in curating high-quality handbags, accessories, and lifestyle pieces that speak to a refined aesthetic. Our mission is to deliver an unparalleled shopping experience that blends design, function, and story — whether in-store or online.
            </p>
        </section>

        <img src="images/shelf-display.jpg" alt="Luxury Handbag Display" class="showcase-image">

        <div class="content-grid">
            
            <div class="text-column">
                <h3>Our Story</h3>
                <p>
                    Britium Gallery was born from a desire to elevate everyday elegance. What began as a boutique concept has evolved into a digital-first brand, bridging traditional craftsmanship with innovative retail experiences. Each piece we feature is handpicked for its artistry, authenticity, and timeless appeal.
                </p>
                <p>
                    Our journey has taken us from intimate showrooms to a scalable eCommerce platform — not to sell more, but to reach further, offering curated luxury to those who appreciate the finer details.
                </p>

                <div class="values-list">
                    <h3>Our Values</h3>
                    
                    <div class="value-item">
                        <span class="value-title">Elegance in Simplicity –</span>
                        <span class="value-desc">We believe true luxury is subtle and refined.</span>
                    </div>

                    <div class="value-item">
                        <span class="value-title">Craftsmanship & Authenticity –</span>
                        <span class="value-desc">Every item we offer reflects intentional design and quality.</span>
                    </div>

                    <div class="value-item">
                        <span class="value-title">Customer-Centric –</span>
                        <span class="value-desc">We exist to inspire confidence and ease through seamless, personalized service.</span>
                    </div>

                    <div class="value-item">
                        <span class="value-title">Innovation with Purpose –</span>
                        <span class="value-desc">From digital design to data-driven retail, we embrace modern tools to enhance our timeless identity.</span>
                    </div>

                    <div class="value-item">
                        <span class="value-title">Sustainability & Responsibility –</span>
                        <span class="value-desc">We aim to curate responsibly, valuing durability, ethics, and meaningful consumption.</span>
                    </div>
                </div>
            </div>

            <div class="image-column">
                <img src="images/balloon-bag-street.jpg" alt="City Street Luxury">
            </div>

        </div>

    </div>

    <footer>
        &copy; 2025 Britium Gallery. All Rights Reserved.
    </footer>

    <div class="chat-widget-btn" id="chatBtn">
        <span class="chat-icon">💬</span>
    </div>

    <div class="chat-window" id="chatWindow">
        <div class="chat-header">
            <h4>Britium Concierge</h4>
            <span class="close-chat" id="closeChat">×</span>
        </div>
        <div class="chat-messages" id="chatMessages">
            <div class="message bot">
                Welcome to Britium Gallery. How may I assist you with your luxury experience today?
            </div>
        </div>
        <div class="chat-input-area">
            <input type="text" class="chat-input" id="chatInput" placeholder="Type your inquiry...">
            <button class="chat-send" id="sendBtn">Send</button>
        </div>
    </div>

    <script>
        const chatBtn = document.getElementById('chatBtn');
        const chatWindow = document.getElementById('chatWindow');
        const closeChat = document.getElementById('closeChat');
        const sendBtn = document.getElementById('sendBtn');
        const chatInput = document.getElementById('chatInput');
        const chatMessages = document.getElementById('chatMessages');

        // Toggle Chat Window
        chatBtn.addEventListener('click', () => {
            chatWindow.classList.add('active');
        });

        closeChat.addEventListener('click', () => {
            chatWindow.classList.remove('active');
        });

        // Send Message Logic
        function sendMessage() {
            const text = chatInput.value.trim();
            if (text) {
                // Add User Message
                addMessage(text, 'user');
                chatInput.value = '';

                // Simulate Bot Response (Simple Logic)
                setTimeout(() => {
                    let botResponse = "Thank you for contacting Britium Gallery. Our concierge team is reviewing your request.";
                    
                    const lowerText = text.toLowerCase();
                    if(lowerText.includes('track') || lowerText.includes('order')) {
                        botResponse = "To track your order, please provide your Order Number (e.g., #12345). You can check the status on our Tracking Page.";
                    } else if (lowerText.includes('return')) {
                        botResponse = "Returns are accepted within 30 days of purchase for unused items. Would you like the link to our Return Request form?";
                    } else if (lowerText.includes('store') || lowerText.includes('location')) {
                        botResponse = "Our flagship store is located at No. 277, Anawrahta Road, East Dagon. We also have locations in Yangon and Mandalay.";
                    } else if (lowerText.includes('hello') || lowerText.includes('hi')) {
                        botResponse = "Hello! I am the Britium Concierge. Are you looking for a specific handbag or accessory today?";
                    }

                    addMessage(botResponse, 'bot');
                }, 800);
            }
        }

        function addMessage(text, sender) {
            const msgDiv = document.createElement('div');
            msgDiv.classList.add('message', sender);
            msgDiv.innerText = text;
            chatMessages.appendChild(msgDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight; // Auto scroll to bottom
        }

        sendBtn.addEventListener('click', sendMessage);
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') sendMessage();
        });
    </script>

</body>
</html>

